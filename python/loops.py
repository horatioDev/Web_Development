# For loops
for i in [0, 1, 2, 3, 4, 5, 6]:
  print(i) # Prints each element in list

# Range
for i in range(6):
  print(i) # Prints 0 - 5

# Define a list of names
names = ['Ray', 'Xavier', 'Mystery', 'Tranq']

for name in names:
  print(name) # Prints each name in list